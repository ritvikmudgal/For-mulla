# Binomial- =BINOM.DIST(k,n,p,FALSE)
#Multinomial Coefficient- =MULTINOMIAL(X1,X2,X3)
#Multinomial Probability- =MULTINOMIAL(X1,X2,X3)*Product(p1^X1,p2^X2,p3^X3)
#Poisson- =POISSON.DIST(k,Lambda,False)
#Geometric- =(1-p)^k*p
#Uniform- =(X2-X1)/(b-a)
#Exponential- =EXPON.DIST(X,Lambda,False)
#Normal - =NORM.DIST(X,Avg,St. Dev, FALSE), Average- =AVERAGE(Range), St. Dev- =STDEV(range)
#Cumulative- =NORM.DIST(X,Avg,St. Dev, TRUE) (for normal)
#Cumulative- -EXPON.DIST(X,Lambda, TRUE) (for exponential)
#Squared Difference- =(B2-C2)^2
#Eucladian distance- =SQRT(SUMXMY2(B2:Bn,C2:Cn))
#Covariance- =Covariance.P(A2:A8,B2:B8)
#Corelation- =CORREL(A2:A8,B2:B8)
#Bernoulli's random var. generation- =BINOM.INV(1,0.5,RAND())
#Binomial Random var. generation- =BINOM.INV(10,0.35,RAND())
#Uniform Distribution random var. generation- =a + RAND() * (b - a)
#Normal Distribution Random var. genration- =NORMINV(RAND(), mean, std_dev)
#log calculation- =LOG(P(x),2)
#Entropy- H(X)= −∑p(x)log2​(p(x))
